{% extends 'base.html.twig' %}

{% block title %}Consumer's list{% endblock %}

{% block body %}
    <h1>Consumser's list</h1>

    {% for consumer in consumers %}
        <a href="{{ path('show_consumer', {'id': consumer.id})}}">{{consumer}}</a> <br>
    {% endfor %}

{% endblock %}