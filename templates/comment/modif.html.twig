{% extends 'base.html.twig' %}

{% block title %}Mo{% endblock %}

{% block body %}
<br><br><br><br>
    <div class="container">
        <br>
        <h3>Change this comment</h3>
        <br>

        {{ form_start(formComment) }}
            {{ form_widget(formComment) }}
            <button class="btn btn-primary">Save</button>
        {{ form_end(formComment) }}
    </div>
    <br>
{% endblock %}